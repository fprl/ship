// Package kernel contains ship's mechanism layer: command registration,
// admission, dispatch, cancellation, events, and namespaced state. It knows
// mechanism only and has no knowledge of domain operations.
package kernel
