name = "hono-api"
box = "deploy@example.com"
probe = "/health"

[processes]
web = { cmd = "bun run src/server.ts", port = 3000, resources = { memory = "256m", cpus = 0.5 } }

[routes]
"api.example.com" = "web"
