name = "php-plain"
box = "example.com"
probe = "/health"

[env]
APP_SECRET = "@secret:APP_SECRET"
DATABASE_PATH = "/data/app.sqlite"

[processes]
web = { port = 8080, resources = { memory = "256m", cpus = 0.5 } }

[routes]
"php.example.com" = "web"
